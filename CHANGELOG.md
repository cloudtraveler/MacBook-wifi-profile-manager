# 변경 이력

이 프로젝트는 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

## [1.2.0]

### 추가
- 프로필에 **위치 정보** 필드 추가 (SSID 다음 칸). 목록에도 `위치` 열로 표시
- CLI `--list` 출력에 위치 열 추가

### 변경
- 열 추가에 따라 Tk 창 기본 크기를 1300×720으로 확대
- 위치 필드가 없는 기존 `profiles.json` 은 빈 값으로 자동 보정 (하위 호환)

## [1.1.0]

### 추가
- **브라우저 UI 모드**(`--web`) — Tk를 전혀 사용하지 않는 로컬 웹 UI
  - 127.0.0.1 바인딩 + 랜덤 토큰 인증, 표준 라이브러리만 사용
  - 스크립트를 ES5로 작성해 구형 Safari에서도 동작
- `--tk` 옵션으로 Tk GUI 강제 실행

### 수정
- **Tk 8.5 환경에서 창은 뜨지만 내용이 그려지지 않는 문제** — Tk 8.6 미만이 감지되면
  자동으로 브라우저 UI로 전환
- Tkinter가 아예 없는 환경에서도 브라우저 UI로 폴백

## [1.0.1]

### 추가
- `--doctor` 실행 환경 진단 명령
- `~/.wifi_profile_manager/app.log`, `launcher.log` 크래시/실행 로깅

### 수정
- 런처가 Tk 8.6 이상 인터프리터를 우선 선택
- Tk 8.6 미만에서 aqua 대신 clam 테마로 폴백 + 강제 리드로우
- 우측 패널 버튼·안내 문구가 잘리던 레이아웃 수정
- CLI 출력이 로그 파일로 리디렉션되던 문제 수정

## [1.0.0]

### 추가
- SSID별 프로필 저장 (DHCP / STATIC)
- 주변 스캔 결과와 대조한 연결 가능 여부 표시 (`● 연결됨` / `○ 연결 가능` / `· 범위 밖`)
- 선택한 프로필로 접속 + 저장된 IP 설정 자동 적용 (3단계 로그)
- macOS 14.4+ 대응 (`airport` 제거 → `system_profiler` 폴백)
- Wi-Fi 비밀번호 macOS 키체인 저장
- IPv4 / 서브넷 마스크 / 게이트웨이 대역 일치 검증
- CLI 모드 (`--list`, `--scan`, `--connect`, `--info`)
- `.app` 번들 및 PyInstaller 완전 독립형 빌드 스크립트
